const Header = () => {
    
    return (
        <header className="w-full h-20 bg-black">
            <nav className="">
            </nav>
        </header>
    );
};

export default Header;